import numpy as np
import matplotlib.pyplot as plt
from scipy import signal 
plt.style.use('seaborn')



def fourier(x,n):
    f = A*abs(sen(w0*t));

T = np.pi;

plt.plot([1, 2, 3, 4])
plt.ylabel('some numbers')
plt.show()